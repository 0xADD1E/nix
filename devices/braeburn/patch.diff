diff --git a/connect/connect.script b/connect/connect.script
index 17dedaf..119b128 100755
--- a/connect/connect.script
+++ b/connect/connect.script
@@ -40,7 +40,7 @@ progress = 0;

 fun refresh_callback ()
   {
-    flyingman_sprite.SetImage(flyingman_image[Math.Int(progress / 2) % 120]);
+    flyingman_sprite.SetImage(flyingman_image[progress % 120]);
     progress++;
   }
